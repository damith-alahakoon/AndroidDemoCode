<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sample.damith.com.mydemocode">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomToastActivity" />
        <activity
            android:name=".JsonRequestActivity"
            android:label="@string/title_activity_json_request"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".RadioBtnCheckBoxActivity" />
        <activity android:name=".QRScanerActivity" />
        <activity android:name=".CustomSpinnerActivity" />
        <activity android:name=".AlertBoxActivity" />
        <activity android:name=".CustomListViewActivity" />
        <activity android:name=".LogFileActivity" />
        <activity android:name=".UploadFileVollyActivity" />
        <activity android:name=".FormValidationActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".QRandBarcodeGenerateActivity" />
        <activity android:name=".CustomStyleActivity"></activity>
    </application>

</manifest>